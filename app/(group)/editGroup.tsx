import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const GroupEdit = () => {
    return (
        <View style={styles.container}>
            <Text>Group Edit</Text>
        </View>
    );
};

export default GroupEdit;

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});
